import * as React from 'react';
import * as ReactDOM from 'react-dom';

import { AppAdmin } from './AppAdmin';

const config = require('tmw-common/config/config');

ReactDOM.render(<AppAdmin />, document.getElementById('root'));
